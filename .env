# Checked into the repo temporarily so production can pull required env vars.
# Tighten this later (gitignore + secrets management).

DATABASE_URL=local.db

# Deploy workflow rewrites these to production values on Lightsail.
ORIGIN=https://sndbnk.com
PUBLIC_BASE_DOMAIN=sndbnk.com

# Behind Caddy/Lightsail these help the Bun adapter reconstruct request URLs.
PROTOCOL_HEADER=X-Forwarded-Proto
HOST_HEADER=X-Forwarded-Host

BETTER_AUTH_SECRET=e2942adaeb997e35aba11d37659d61988dfbc461cc16386b0668fcb070d91efc

MEDIA_ROOT=./media
BODY_SIZE_LIMIT=110M
STORAGE_SECRET=b5b6d88d6933c07be131cc849f97f21e826824b4848db1091fb4ae23b2caf871

# Must cover audio max (100MB) + cover max (5MB) + form overhead.
BODY_SIZE_LIMIT=110M

# ---------------------------------------------------------------------------
# Billing (live) — get keys from:
# https://dashboard.stripe.com/acct_1NPVldy1NMF8U0kZcI0z/apikeys
# Prefer a restricted live key (rk_live_...) over sk_live_...
# ---------------------------------------------------------------------------
STRIPE_SECRET_KEY=""
PUBLIC_STRIPE_PUBLISHABLE_KEY=""

# Create the endpoint first, then paste its signing secret:
# Dashboard → Developers → Webhooks → Add endpoint
#   URL: https://sndbnk.com/api/stripe/webhook
#   Events: checkout.session.completed,
#           customer.subscription.created,
#           customer.subscription.updated,
#           customer.subscription.deleted,
#           invoice.payment_failed
STRIPE_WEBHOOK_SECRET=""

# ---------------------------------------------------------------------------
# Mail — console logs to journalctl until you flip to smtp
# -----------------------------------------------------------------
MAIL_TRANSPORT=smtp
MAIL_FROM="SNDBNK <no-reply@sndbnk.com>"

# Fill these and set MAIL_TRANSPORT=smtp when ready:
SMTP_HOST="756xrni6zsf4.fips.wmjb.mail-manager-smtp.amazonaws.com"
SMTP_PORT=587
SMTP_SECURE=false
SMTP_USER="inp-owun6nkanvubp6c4yqlfam7m"
SMTP_PASSWORD="rKS0@%R0tuBOD)&,8LOdR=9CJ$}|sPj}"
